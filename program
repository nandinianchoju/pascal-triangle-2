pascal triangle

to print the elements of an entire row if row number is given

def generate(row,col):
  n=row-1
  r=col-1
  ans=1
  for i  in range (r): 
    ans=ans*(n-i)
    ans=ans//(i+1)
  print(ans,end=" ")
row=int(input())
for col in range(1,row+1):
 generate(row,col)
